# DRL With MLP Result Archive

This directory mirrors the stable paper-facing outputs for the `DRL + MLP` line.

## Structure

- `snapshots/`
  - timestamped markdown summaries for the latest comparable batches

## Source of Truth

- Writing drafts still live in `project_document/paper/DrlWithMlp/`.
- Once a snapshot is considered stable enough for archival use, it should also be copied here.
